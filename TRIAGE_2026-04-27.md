# Post-PR-#64 Triage — 2026-04-27

> Author: AI Developer (genspark_ai_developer)
> Production: https://mba-agent-71hu.onrender.com
> Live snapshot taken: 2026-04-27 23:11 IST
> Build verified: ✅ vite build OK, ✅ python syntax OK

This document explains exactly **why your "For You" tab shows
irrelevant jobs** after the PR #64 merge, what I changed in this
follow-up patch, and the **manual steps you still need to do** in
the Render and Supabase dashboards.

---

## 🔬 Root cause #1 — your CV is not on the server

**What I observed (live, just now):**
```
GET https://mba-agent-71hu.onrender.com/api/cv/status?telegram_id=1284690336
{
  "data": {
    "has_cv": false,        ← THE BUG
    "keyword_count": 0,
    "top_keywords": [],
    "has_profile": false
  }
}
```

The "For You" engine is healthy — it just has nothing to match
against. With no CV on disk, every listing receives a placeholder
score of `50.0`. The list then sorts by that constant, which
means the order is effectively random — that's exactly the
"Tally Accountant — Agra" / "Public Relations — Bangalore"
garbage you're seeing.

**Why the CV is missing even though you uploaded it:**

Render's **free tier filesystem is ephemeral.** The original
upload code in `core/miniapp_api.py` writes the CV to:

```
data/user_cvs/{telegram_id}.pdf
```

Render wipes the entire `data/` directory on every redeploy and
on every cold restart (which on the free tier happens whenever
the dyno has been idle for 15 minutes). Combined with
`FORCE_DB_RESET=true` in your env (visible in your screenshot
and confirmed in the boot log: `*** FORCE_DB_RESET=true detected ***`),
the user-uploaded CV vanishes silently after every deploy or
restart, and the front-end sends the user back to a neutral
state with no warning.

---

## 🔬 Root cause #2 — `FORCE_DB_RESET=true` is left enabled

From the production log:
```
2026-04-27 17:25:46 | WARNING | __main__:_init_data_seed:446
[Phase 3] *** FORCE_DB_RESET=true detected ***
[Phase 3] SQLite wiped: {ghost_scores: 0, application_packages: 0,
                          auto_apply_queue: 0, outcomes: 0,
                          clean_listings: 0, raw_listings: 0}
```

This wipes the **local** SQLite cache on every boot. Supabase
data is safe (the safety gate works), but you lose:
- the freshly-scraped listings from the previous scheduler run
- the CV-match scores that have already been computed
- the auto-apply queue

After that wipe, the system has to scrape from scratch again,
which is why your `clean_listings` was 0 right after boot.

**Action required from you:** Remove `FORCE_DB_RESET` from
Render's Environment tab. **Do this before anything else** —
otherwise every deploy of this patch will also re-wipe.

---

## 🔬 Root cause #3 — Internshala scraper writes garbage rows

From the live `/api/supabase/latest-jobs` response:

```json
{
  "title":   "Tally Accountant",
  "company": "Tally Accountant",   ← same string!
  "sourceUrl": "https://internshala.com/.../at-expertbells-consulting...",
  "description": ""
}
```

The actual employer (`Expertbells Consulting Private Limited`) is
in the URL slug but never extracted. This is a separate issue from
the "For You" bug — it makes the **All Jobs** tab look bad too. I
have NOT fixed this in this patch; I'll attack it in a follow-up
because it requires a careful Internshala-API replay session.

---

## ✅ What I changed in this PR

| Change | File | Why |
|--------|------|-----|
| New module `cv_storage.py` | `core/cv_storage.py` | Mirrors uploaded CVs to a Supabase `user_cvs` table so they survive restart |
| Schema migration | `data/cv_storage_schema.sql` | One-time SQL to create the `user_cvs` table |
| Upload endpoint mirrors to Supabase | `core/miniapp_api.py` (`handle_user_upload_cv`) | Every successful upload now writes to both disk AND Supabase |
| Cold-start auto-restore | `core/cv_matcher.py` (`refresh_from_cv`) | When the local PDF is missing, transparently restore it from Supabase before returning `has_cv=false` |
| `/api/cv/status` exposes backup state | `core/miniapp_api.py` (`handle_cv_status`) | Front-end can now show a "your CV is/is not backed up" warning |
| Sane fallback ordering when no CV | `core/miniapp_api.py` (`handle_cv_matched_jobs`) | When `has_cv=false`, sort by (description-richness, stipend desc, recency) instead of a constant 50 → no more random Tally Accountant |
| Louder `FORCE_DB_RESET` warning | `main.py` (`_init_data_seed`) | Boot log now screams when destructive boot is enabled |
| `render.yaml` doc update | `render.yaml` | Replaces the misleading comment that made it look safe to leave on |
| `CVStatus` TypeScript type | `mini-app/src/services/api.ts` | Adds `remote_backup` + `warning` fields |

The mini-app build still produces a clean `dist/` (verified) and
all touched Python files parse (verified).

---

## 🛠 Operator action items — do these in this exact order

### 1. Run the Supabase migration (≈ 30 seconds)

1. Open https://supabase.com/dashboard/project/bclqnfryfondvypzivrx
2. Click **SQL Editor** → **New query**
3. Copy-paste the contents of `data/cv_storage_schema.sql`
4. Click **Run**
5. Verify the `user_cvs` table appeared under **Table Editor**

### 2. Remove `FORCE_DB_RESET` from Render (10 seconds)

1. Open https://dashboard.render.com/web/srv-d6mser3hf6gs73btold0/env
2. Find the row `FORCE_DB_RESET`
3. Click the trash-can icon → **Save changes**
4. Render will redeploy automatically

### 3. Wait for the deploy of this PR

After PR is merged you'll see the new boot log:
```
[Phase 3] !!! FORCE_DB_RESET=true — DESTRUCTIVE BOOT !!!
```
should now be **absent** — meaning step 2 worked.

### 4. Re-upload your CV through the mini-app

1. Open the bot → `/miniapp` → **Settings** tab
2. Tap **Upload CV** → pick your latest PDF
3. Watch for the success toast
4. Switch to **For You** tab — you should now see jobs sorted by
   how well they match your CV (skills/keywords from your resume
   appear in the matched-keywords pill)

### 5. Sanity-check the persistence fix

After step 4 has succeeded, manually trigger a Render restart:
1. Render Dashboard → **Manual Deploy** → **Clear build cache & deploy**
2. Wait ~5 min for boot
3. Open the mini-app → **For You** tab — should still rank
   against your CV (instead of falling back to "Upload your CV").
4. Confirm via curl:
   ```bash
   curl https://mba-agent-71hu.onrender.com/api/cv/status?telegram_id=1284690336
   # expect: { "has_cv": true, "keyword_count": >0, "remote_backup": true }
   ```

### 6. (Optional, for NEXUS L0–L9 dashboard)

The boot log shows:
```
NEXUS_CALLBACK_SECRET not set — generated ephemeral secret.
Inline buttons will invalidate on restart.
```

If you want NEXUS Telegram inline buttons to keep working across
restarts, add to Render env:
```
NEXUS_CALLBACK_SECRET=<32-char random hex>
```
You can generate one with:
```bash
python -c "import secrets; print(secrets.token_hex(32))"
```

---

## 🚧 Known issues NOT fixed in this PR (next sprints)

| Issue | Severity | Plan |
|-------|----------|------|
| Internshala rows store `company == title` | Medium | Re-walk the parser; needs a live scrape replay |
| `latest_jobs.company_logo`, `company_size` always EMPTY | Low | Same root cause — fix the scraper enrichment |
| `[A-16] Telethon NOT configured` | Optional | Add `TG_API_ID + TG_API_HASH` if you want the userbot listener |
| `sentence-transformers not installed` (Layer 4 semantic dedup) | Low | By design — too heavy for free 512MB tier |
| `cv_match_score` blend with pgvector embedding | Future | Requires embeddings to be backfilled into Supabase first |

---

## 📊 Live system snapshot (taken just before this patch)

```json
GET /health
{ "status": "healthy", "uptime": "19m", "memory": "214 MB / 512 MB",
  "subsystems": { "scheduler": true, "telegram": true,
                  "database": true, "supabase": "operational" } }

GET /nexus
{ "version": "0.2.0",
  "layers_ok": ["L0_vault", "L1_stealth_triad", "L3_scoring",
                "L7_dedup", "L6_orchestrator", "L0a_access",
                "L0b_cv_intake", "L9_dashboard", "L3b_scoring_loop"],
  "layers_fail": {}, "triad_live": true,
  "portals": ["linkedin", "internshala", "naukri", "iimjobs",
              "unstop", "wellfound", "indeed", "ycombinator",
              "instahyre", "shine", "timesjobs"] }
```

**Verdict:** the architecture from PR #64 is up and running
correctly. The "For You" disappointment is purely the missing
CV. Once you complete the 6 operator steps above, the tab will
behave exactly the way you expected.
